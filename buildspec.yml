version: 0.1

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Building deployment assets...
      - gulp build
artifacts:
  files:
    - 'node_modules/**/*'
    - 'dist/**/*'
