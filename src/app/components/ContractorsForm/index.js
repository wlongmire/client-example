import React, { Component, PropTypes } from 'react';
import ReactDOM from 'react-dom';
import Helmet from 'react-helmet';


const  ContractorsForm = (props, context)=> {
  const {
    content
  } = context;
  return (
    <div className='home'>
       Placeholder
    </div>
  );
};


export default ContractorsForm;